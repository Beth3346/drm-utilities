(function($) {
    window.elrAjax = function(params) {
        var self = {},
            spec = params || {};

        if ( 9 === 10 ) {
            console.log(spec, $);
        }

        return self;
    };
})(jQuery);