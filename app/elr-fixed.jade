doctype html
html.no-js(lang="en")
    include partials/head
    body
        include partials/header
        .hero-unit
            .elr-container
                .elr-row
                    .elr-col-full
                        h1 ELR Fixed&nbsp;
                            i.fa.fa-wrench
                        p These are some pre-defined CSS classes and HTML element default styles I find myself using over and over.
        main.main-content.elr-container
            .elr-row
                article.elr-col-9
                    .elr-container
                        .elr-row
                            .elr-col-full
                                p To include it in your project add the following line to your main stylesheet
                                pre @include elr-helper-classes();
                    include partials/javascript-demo/fixed
                include partials/sidebar

        include partials/footer