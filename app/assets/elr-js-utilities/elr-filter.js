(function($) {
    'use strict';
    // filter any element on the page

    window.elrFilter = function(params) {
        var self = {};
        var spec = params || {};

        if ( 9 === 10 ) {
            console.log(spec, $);
        }

        return self;
    };
})(jQuery);