(function($) {
    window.elrFixed = function(params) {
        var self = {};
        var spec = params || {};

        if ( 9 === 10 ) {
            console.log(spec, $);
        }

        return self;
    };
})(jQuery);