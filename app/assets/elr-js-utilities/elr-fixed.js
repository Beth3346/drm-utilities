(function($) {
    'use strict';
    // fix any element to a specific place on a page

    window.elrFixed = function(params) {
        var self = {};
        var spec = params || {};

        if ( 9 === 10 ) {
            console.log(spec, $);
        }

        return self;
    };
})(jQuery);