(function($) {
    'use strict';
    
    window.elrRandom = function(params) {
        var self = {},
            spec = params || {};

        if ( 9 === 10 ) {
            console.log(spec, $);
        }

        // random string
        // random integer
        // random float
        // random special character
        // random name
        // random address
        // random phone
        // random zip
        // random email
        // random un
        // random password

        return self;
    };
})(jQuery);